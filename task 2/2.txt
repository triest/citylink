SELECT * FROM `subscriptions` `sub` 
	left JOIN `reader` `red` on `sub`.`reader_id`=`red`.`id` 
    LEFT join `magazine` `mag` on `sub`.`magazine_id`=`mag`.`id`
    WHERE `mag`.`name`='Мурзилка'
     AND  TIMESTAMPDIFF(YEAR, `red`.`date_of_birth`, CURDATE())>30